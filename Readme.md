## Weekday Aggregation Function

This project contains a JavaScript function that aggregates values by weekday
from a date-value dictionary and fills missing weekdays using the mean of
adjacent days.

### Run Tests
npm install
npm test
